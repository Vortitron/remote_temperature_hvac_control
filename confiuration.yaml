# Define the desired temperature input
input_number:
  sam_desired_temperature:
    name: "Sam Desired Temperature"
    min: 16
    max: 30
    step: 0.5
    unit_of_measurement: "°C"
    initial: 20
input_text:
  sam_hvac_status:
    name: "Sam HVAC Status"
    max: 255  # Max length of the status message
    initial: "Initializing..."
